# ============================================
# Authentication Configuration
# ============================================

# AUTH_SECRET - Required for NextAuth v5 session encryption
# Generate a strong random secret with: openssl rand -base64 32
# CRITICAL: Keep this secret and never commit it to git
AUTH_SECRET=your_generated_secret_here

# ============================================
# Google OAuth Configuration
# ============================================

# Get these credentials from: https://console.cloud.google.com/
# 1. Go to APIs & Services > Credentials
# 2. Create OAuth 2.0 Client ID (Web application)
# 3. Add authorized redirect URI: https://your-domain.com/api/auth/callback/google

GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# ============================================
# Application URL Configuration
# ============================================

# NEXTAUTH_URL - The base URL where your application is hosted
# For local development: http://localhost:3000
# For production: https://your-domain.com
# IMPORTANT: Update Google OAuth redirect URIs when changing this

NEXTAUTH_URL=https://your-domain.com

# ============================================
# Setup Instructions
# ============================================
#
# 1. Copy this file to .env.local:
#    cp .env.local.example .env.local
#
# 2. Generate AUTH_SECRET:
#    openssl rand -base64 32
#    Paste the result into AUTH_SECRET
#
# 3. Set up Google OAuth:
#    - Follow docs/GOOGLE_OAUTH_SETUP.md
#    - Add your credentials above
#
# 4. Update NEXTAUTH_URL to your actual domain
#
# 5. NEVER commit .env.local to git!
#    (It's already in .gitignore)
#
# ============================================
